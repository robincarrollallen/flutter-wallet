# 本机配置。复制成 .env 后填值：`cp .env.example .env`
# .env 不进 git，但它被声明成了 asset，文件必须存在，值可以留空。

# Etherscan V2 的 API key（https://etherscan.io/apidashboard 免费申请）。
# 一个 key 覆盖全部 EVM 链。留空则 EVM 链不查远程历史，只显示本机发出的交易；
# 其余链（Solana / Tron / Bitcoin）不需要 key，照常工作。
ETHERSCAN_API_KEY=
